import 'package:flutter/material.dart';

class AlimentacionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Alimentación")),
      body: Center(child: Text("Pantalla de Alimentación active blend")),
    );
  }
}
